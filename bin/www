#!/bin/env node

var app = require('./../server');

app.listen(process.env.PORT || 3000, process.env.IP || "0.0.0.0", function(){
  console.log("Server listening.. ");
});

//did chmod +x bin/www to make this file executable