define([], function() {
    return {
        auth: {
            TOKEN_KEY: 'authToken',
            USER_KEY: 'userId'
        },
        extraInfo:{
            unsavedPoll: 'unsavedPoll'
        },
        urls: {
            AUTHENTICATE: '/api/authenticate'
        }
    }
});